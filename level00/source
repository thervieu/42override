#include <stdio.h>
#include <string.h>

int main(int ac, char **av) {

    puts("***********************************");
    puts("* 	     -Level00 -		  *");
    puts("***********************************");
    printf("Password:");
    int var;
    scanf("%d", var); // asssuming 'var' is the next registery

    if (var == 5276) {
        puts("\nAuthenticated!");
        system("/bin/sh");
    }
    else {
        puts("\nInvalidPassword")
    }
    return 0;
}